// Site configuration — update before launch.

// Premium checkout link (e.g. a Stripe Payment Link or your store URL).
// While this is an empty string the "Buy Premium" button renders disabled with
// a tooltip, so nothing links to a dead page. Paste the real URL here to enable it.
export const PREMIUM_CHECKOUT_URL = "";
